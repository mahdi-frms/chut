import { controllersMessage } from './controllers-message';

describe('controllersMessage', () => {
  it('should work', () => {
    expect(controllersMessage()).toEqual('controllers-message');
  });
});
